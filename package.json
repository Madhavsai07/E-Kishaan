{
  "name": "e-kishaan-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd HACKATHON/backend && npm install && npm run build",
    "build:all": "cd HACKATHON/frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "build:backend": "cd HACKATHON/backend && npm install && npm run build",
    "dev": "cd HACKATHON/frontend && npm run dev",
    "start": "cd HACKATHON/backend && node dist/server.js"
  }
}
